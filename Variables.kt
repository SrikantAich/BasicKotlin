/*
var and val are used  to create variables
var can be reassigned(mutable)
val can not be reassigned(immutable)
*/
fun main()
{
    // val a=10;
    // a=20; cannot be reassigned
    var number:Int;
    number=10+20;
    println(number)
    number=15;
    println(number);

}